interface:
  display_name: "Paperclip Project Hygiene"
  short_description: "Execute routine work and approve only core gates"
  default_prompt: "Use $isolate-paperclip-work to execute routine recoverable operations directly, route only core decision gates through Paperclip board approval, keep all operational work agent-owned, verify delivery, and clean up the local session."
